# Login Flow (Monorepo)

This repo contains:

- `client/`: React (Vite)
- `server/`: NestJS API

## Getting started

### Client

```bash
cd client
npm install
npm run dev
```

### Server

```bash
cd server
npm install
npm run start:dev
```

